const serverUrl = 'https://mrbitex.net'
const apiVersion = 'v1'
const baseUrl = `${serverUrl}/api/${apiVersion}`
const genralUrl = `${baseUrl}/general`

export const currencyUrl = `${genralUrl}/currency`