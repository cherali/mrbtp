const serverUrl = 'https://mrbitex.net'
const apiVersion = 'v1'
const baseUrl = `${serverUrl}/api/${apiVersion}`

export const generalUrl = `${baseUrl}/general`

export const currencyUrl = `${generalUrl}/currency`
export const tradeUrl = `${generalUrl}/trade`